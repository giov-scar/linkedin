export interface Icommentapi {
   comment: string;
   rate: number;
   elementId: string;
   _id:string;
  // author:string;
  // comment:string;
  // createdAt: string;
  // elementId: string;
  // rate: number;
  // updatedAt: string;
  // __v: number;
  // _id: string;
}
